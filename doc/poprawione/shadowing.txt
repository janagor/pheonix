fn casted_structure() {
    fn scoped() {
        "in scope"
    }

    struct Rectangle {
        fn __init__(width, height) {
            self.width = width;
            self.height = height;
        }

        fn get_area() {
            width * height
        }
        fn get_perimeter() {
            let result = 2 * (width * height);
            result
        }
    }

    let rectangle = Rectangle(12, 13); // ok
    scoped(); // ok
}

scoped();
// 'error: cannot find "scoped()" structure in the scope.'
let wrong_rectangle = Rectangle(12, 13);
// 'error: cannot find "Rectangle" structure in the scope.'

